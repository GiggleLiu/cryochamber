{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Console window: native store + unrestricted hub HTTP. The allow list is URLPattern, not globs: a hostname pattern with no port means the scheme's DEFAULT port only, so `http://**` would refuse every hub on 8765. The `:*` is what makes the port a wildcard. Guarded by tests/capability_http_scope.rs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default",
    {
      "identifier": "http:default",
      "allow": [{ "url": "http://*:*" }, { "url": "https://*:*" }]
    }
  ]
}
