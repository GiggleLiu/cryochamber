{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Console window: native store + unrestricted hub HTTP",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default",
    {
      "identifier": "http:default",
      "allow": [{ "url": "http://**" }, { "url": "https://**" }]
    }
  ]
}
