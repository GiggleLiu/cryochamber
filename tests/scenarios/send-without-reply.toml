description = "Agent sends a status update but does not receive/reply to the queued inbox message; it can only end the session by reporting a failure, because the quietness gate refuses a clean hibernate while mail is unread."

[[actions]]
kind = "send"
message = "Status update for operator"

[[actions]]
kind = "hibernate"
exit = 1
summary = "Sent a status update only"
