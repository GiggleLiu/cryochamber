# cryo.toml — Cryochamber project configuration

# Agent command (e.g. "opencode", "claude", "codex", "pi", "kimi")
agent = "{{agent}}"

# Session timeout in seconds (0 = no timeout)
max_session_duration = 3600

# Default timeout in seconds for `cryo-agent receive --wait` (interactive
# mode). The daemon clamps any value to 1-86400 seconds (24 hours).
# wait_timeout = 14400

# Reply window in seconds. After the agent hibernates, the daemon keeps
# the session parked this long; a message arriving inside the window is
# handled by the same session. Unset = 300 (5 minutes); 0 disables the
# window. The unread-inbox hibernate gate applies regardless of this setting.
# reply_window = 600

# Directories the daemon watches for new files to wake the agent reactively.
# Paths are interpreted relative to the chamber directory (this directory)
# unless they are absolute. Set to [] to disable reactive wake entirely.
watch_dirs = ["messages/inbox"]

# Sync polling interval in seconds (for cryo-zulip sync)
# zulip_poll_interval = 5

# Provider environment injected into the agent process.
# WARNING: If you add API keys below, ensure cryo.toml is in your .gitignore
# to avoid accidentally committing secrets.
#
# [provider]
# name = "opencode"
# env = { OPENCODE_PROVIDER = "anthropic", OPENCODE_MODEL = "claude-sonnet-4-20250514", ANTHROPIC_API_KEY = "sk-ant-..." }
