# cryo.toml — Chess by Mail

# Agent command (e.g. "opencode", "claude", "codex")
agent = "opencode"

max_retries = 1

# Session timeout in seconds (0 = no timeout)
max_session_duration = 0

# Agent checks on its own schedule; inbox messages are picked up at next wake
watch_inbox = false
